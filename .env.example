# Copy this file to a protected server-side environment file (mode 600).
# Do not commit real credentials. These values are placeholders only.
#
# Recommended locations on the CentOS development VM:
#   /etc/announcement-board.env
#   /home/haha/.config/announcement-board/env

DB_URL=jdbc:mysql://127.0.0.1:3306/announcement_board?useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci
DB_USERNAME=announcement
DB_PASSWORD=change-me
